kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-db-data
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: pv-db-data
  storageClassName: azureblob-nfs-premium