class Export:
    def __init__(self, item, index, pipeline, limit, timedelta):
        self.item = item
        self.index = index
        self.pipline = pipeline
        self.limit = limit
        self.timedelta = timedelta
